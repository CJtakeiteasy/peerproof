# Lighthouse analysis

This repository contains the computational materials for the Lighthouse study.
Use project metadata, source dependencies, and machine-readable output behavior
to identify the workflow that produces the paper's selected quantitative claim.

The repository does not compute robustness diagnostics. Those belong to an
independent verifier, rather than to the analysis under review.
