const preview = {
  title: "Lighthouse study descriptive preview",
  note: "Illustrative display metadata only; this file does not emit the claim artifact.",
};

process.stdout.write(`${JSON.stringify(preview)}\n`);
