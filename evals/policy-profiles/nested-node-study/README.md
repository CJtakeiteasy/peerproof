# Nested Node policy evaluation

This checked-in fixture has a repository root above an `analysis/` working
directory and a nested `workflow/reproduce.mjs` entry point. It proves that
`NodeScriptPolicy v1` is reusable across more than the Lighthouse
`node analysis.js` layout. It is reviewed fixture code, not an arbitrary upload.
